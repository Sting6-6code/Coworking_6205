<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns="http://javafx.com/javafx/23.0.1"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="application.AdminBillingController"
      spacing="20" alignment="TOP_CENTER" prefWidth="1000" prefHeight="600"
      style="-fx-padding: 20;">

    <Label text="All Users Billing Records" style="-fx-font-size: 22px; -fx-font-weight: bold;"/>

    <!-- Filter section -->
    <HBox spacing="10" alignment="CENTER_LEFT">
        <Label text="Filter by User:"/>
        <ChoiceBox fx:id="userFilterChoice" prefWidth="200"/>
        <Label text="Filter by Type:"/>
        <ChoiceBox fx:id="typeFilterChoice" prefWidth="150"/>
        <Button text="Refresh" onAction="#refreshData"/>
    </HBox>

    <!-- Transaction table -->
    <TableView fx:id="transactionTable" prefWidth="950" prefHeight="400">
        <columns>
            <TableColumn fx:id="colUserId" text="User ID" prefWidth="150"/>
            <TableColumn fx:id="colUsername" text="Username" prefWidth="120"/>
            <TableColumn fx:id="colDate" text="Date" prefWidth="120"/>
            <TableColumn fx:id="colType" text="Type" prefWidth="120"/>
            <TableColumn fx:id="colDescription" text="Description" prefWidth="250"/>
            <TableColumn fx:id="colAmount" text="Amount" prefWidth="120"/>
        </columns>
    </TableView>

    <!-- Summary section -->
    <HBox spacing="20" alignment="CENTER_RIGHT" style="-fx-padding: 10;">
        <Label text="Total Revenue:" style="-fx-font-size: 16px; -fx-font-weight: bold;"/>
        <Label fx:id="totalRevenueLabel" text="$0.00" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #2e7d32;"/>
    </HBox>

</VBox>
